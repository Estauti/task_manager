<p id="notice"><%= notice %></p>

<h2><%= @responsible.name %></h2>

<div class="mt-5">
  <%= link_to 'Back', responsibles_path, class: "mr-2" %>
  <%= link_to 'Edit', edit_responsible_path(@responsible), class: "btn btn-primary mr-2" %>
  <%= link_to 'Delete',  @responsible, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger mr-2" %>
</div>
